# RP Platform

Initial TypeScript workspace for a chat-first AI roleplay platform.

Current scope:

- domain model package
- API contracts package
- prompt pipeline package
- API service scaffold

This workspace is intentionally small at the start. The first priority is stabilizing product semantics and backend contracts.
