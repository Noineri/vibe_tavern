// Tailwind v4 is now handled by @tailwindcss/vite plugin (see vite.config.ts).
// No PostCSS plugins needed — Vite plugin handles CSS processing, prefixing, and nesting.
export default {
  plugins: {},
};
