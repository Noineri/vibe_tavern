{
  "name": "@vibe-tavern/db",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@vibe-tavern/domain": "0.3.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.10",
    "drizzle-kit": "^0.28.0"
  }
}
