{
  "name": "@vibe-tavern/import-export",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "smoke:fixtures": "bun run build && bun ../../out/packages/import-export/fixture-smoke.js",
    "test": "bun test"
  },
  "dependencies": {
    "@vibe-tavern/domain": "0.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.10"
  }
}
